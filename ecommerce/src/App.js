const { BrowserRouter, Routes, Route } = require("react-router-dom");

function App() {
  return (
    <div className="App">
      <BrowserRouter>
        <Routes>
          <Route path="" component={} />
        </Routes>
      </BrowserRouter>
    </div>
  );
}

export default App;
